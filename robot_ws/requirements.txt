# robot_ws/requirements.txt — 手寫維護，不要用 pip freeze 覆蓋
# 依據：2026-08-10 對 src/ + collect_data/ 全部 .py 的 import 掃描
# 版本來源：實驗室工作站 freeze（已備份為 requirements.txt.backup）
#
# 不要加進來的東西：
#   - conda 已管（Dockerfile 步驟 6）：grpcio, open3d, pyrealsense2, kachaka-api
#   - ROS/apt 提供：rclpy, sensor_msgs, cv_bridge, tf2_*, launch* 等
#   - workspace 內部套件：kachaka_interfaces, mm_interface, object_query_interfaces,
#     semantic_slam_interfaces, kachaka_nav, stretch（vendor 在 decision_maker 裡）

# --- 數值 / 視覺 ---
numpy==1.26.4
opencv-contrib-python==4.11.0.86
scipy==1.15.3
scikit-learn==1.7.2
matplotlib==3.5.1
pillow==12.0.0
transforms3d==0.4.2

# --- 語音（decision_maker audio 節點）---
openai-whisper==20250625
torch==2.8.0            # whisper 的依賴，顯式釘住以求跨機一致
PyAudio==0.2.14         # 需要 apt: portaudio19-dev
sounddevice==0.5.2
simpleaudio==1.0.4      # 需要 apt: libasound2-dev（從原始碼編譯）

# --- 工具 ---
pyyaml==6.0.2           # freeze 是 5.4.1，但它在 py3.10 裝不起來（無 cp310 wheel），升版
tqdm==4.57.0
keyboard==0.13.5
overrides==7.7.0
wget==3.2               # freeze 裡沒有，PyPI 唯一現行版本

# --- 測試（colcon test 需要，conda env 裡要有自己的 pytest）---
pytest==6.2.5
